// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.security.Principal;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import mwk.pptp.PrincipalStore;
import mwk.pptp.PropertySearch;
import mwk.pptp.Search;
import mwk.pptp.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>CompoundMaster</code>.
 * </p>
 *
 * @see CompoundMaster
 */
public abstract class CompoundMasterDaoBase
    extends HibernateDaoSupport
    implements CompoundMasterDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CompoundMasterImpl.class, id);
        return transformEntity(transform, (CompoundMaster)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster get(Long id)
    {
        return (CompoundMaster)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CompoundMasterImpl.class, id);
        return transformEntity(transform, (CompoundMaster)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster load(Long id)
    {
        return (CompoundMaster)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CompoundMaster> loadAll()
    {
        return (Collection<CompoundMaster>) this.loadAll(CompoundMasterDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(CompoundMasterDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(CompoundMasterImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster create(CompoundMaster compoundMaster)
    {
        return (CompoundMaster)this.create(CompoundMasterDao.TRANSFORM_NONE, compoundMaster);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final CompoundMaster compoundMaster)
    {
        if (compoundMaster == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.create - 'compoundMaster' can not be null");
        }
        this.getHibernateTemplate().save(compoundMaster);
        return this.transformEntity(transform, compoundMaster);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CompoundMaster> create(final Collection<CompoundMaster> entities)
    {
        return (Collection<CompoundMaster>) create(CompoundMasterDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<CompoundMaster> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CompoundMaster entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster create(
        String pptpIdentifier,
        Double solidDose,
        String concentrationUnit,
        String treatmentRoute,
        String schedule,
        String vehicle,
        Double allDose)
    {
        return (CompoundMaster)this.create(CompoundMasterDao.TRANSFORM_NONE, pptpIdentifier, solidDose, concentrationUnit, treatmentRoute, schedule, vehicle, allDose);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        String pptpIdentifier,
        Double solidDose,
        String concentrationUnit,
        String treatmentRoute,
        String schedule,
        String vehicle,
        Double allDose)
    {
        CompoundMaster entity = new CompoundMasterImpl();
        entity.setPptpIdentifier(pptpIdentifier);
        entity.setSolidDose(solidDose);
        entity.setConcentrationUnit(concentrationUnit);
        entity.setTreatmentRoute(treatmentRoute);
        entity.setSchedule(schedule);
        entity.setVehicle(vehicle);
        entity.setAllDose(allDose);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(CompoundMaster compoundMaster)
    {
        if (compoundMaster == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.update - 'compoundMaster' can not be null");
        }
        this.getHibernateTemplate().update(compoundMaster);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<CompoundMaster> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CompoundMaster entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(CompoundMaster compoundMaster)
    {
        if (compoundMaster == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.remove - 'compoundMaster' can not be null");
        }
        this.getHibernateTemplate().delete(compoundMaster);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.remove - 'id' can not be null");
        }
        CompoundMaster entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<CompoundMaster> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CompoundMaster.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>CompoundMasterDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link CompoundMasterDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see CompoundMasterDao#transformEntity(int, CompoundMaster)
     */
    public Object transformEntity(final int transform, final CompoundMaster entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case CompoundMasterDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case CompoundMasterDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see CompoundMasterDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link CompoundMaster}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof CompoundMaster)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return CompoundMaster
     */
    protected CompoundMaster toEntity(Object[] row)
    {
        CompoundMaster target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof CompoundMaster)
                {
                    target = (CompoundMaster)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CompoundMasterImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(CompoundMasterDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CompoundMasterImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<CompoundMaster> search(final Search search)
    {
        return (Set<CompoundMaster>) this.search(CompoundMasterDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniquePptpIdentifier(final String pptpIdentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("pptpIdentifier",pptpIdentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniquePptpIdentifier(final int transform, final String pptpIdentifier)
    {
        final CompoundMaster entity=this.searchUniquePptpIdentifier(pptpIdentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueSolidDose(final Double solidDose)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("solidDose",solidDose,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSolidDose(final int transform, final Double solidDose)
    {
        final CompoundMaster entity=this.searchUniqueSolidDose(solidDose);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueConcentrationUnit(final String concentrationUnit)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("concentrationUnit",concentrationUnit,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueConcentrationUnit(final int transform, final String concentrationUnit)
    {
        final CompoundMaster entity=this.searchUniqueConcentrationUnit(concentrationUnit);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueTreatmentRoute(final String treatmentRoute)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("treatmentRoute",treatmentRoute,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueTreatmentRoute(final int transform, final String treatmentRoute)
    {
        final CompoundMaster entity=this.searchUniqueTreatmentRoute(treatmentRoute);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueSchedule(final String schedule)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("schedule",schedule,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSchedule(final int transform, final String schedule)
    {
        final CompoundMaster entity=this.searchUniqueSchedule(schedule);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueVehicle(final String vehicle)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("vehicle",vehicle,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueVehicle(final int transform, final String vehicle)
    {
        final CompoundMaster entity=this.searchUniqueVehicle(vehicle);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CompoundMaster searchUniqueAllDose(final Double allDose)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("allDose",allDose,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CompoundMaster> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueAllDose(final int transform, final Double allDose)
    {
        final CompoundMaster entity=this.searchUniqueAllDose(allDose);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}
// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: SpringDao.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.util.Collection;
import java.util.Set;
import mwk.pptp.Search;
import org.andromda.spring.PaginationResult;

/**
 *
 * @see MouseCrosstab
 */
public interface MouseCrosstabDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes no transformation will occur.
     */
    public static final int TRANSFORM_NONE = 0;


    /**
     * Transforms the given results to a collection of {@link MouseCrosstab}
     * instances (this is useful when the returned results contains a row of data and you want just entities only).
     *
     * @param results the query results.
     */
    public void toEntities(final Collection<?> results);

    /**
     * Gets an instance of MouseCrosstab from the persistent store.
     * @param id
     * @return MouseCrosstab
     */
    public MouseCrosstab get(Long id);

    /**
     * <p>
     * Does the same thing as {@link #get(Long)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to get.
     * @return either the entity or the object transformed from the entity.
     */
    public Object get(int transform, Long id);

    /**
     * Loads an instance of MouseCrosstab from the persistent store.
     * @param id
     * @return MouseCrosstab
     */
    public MouseCrosstab load(Long id);

    /**
     * <p>
     * Does the same thing as {@link #load(Long)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, Long id);

    /**
     * Loads all entities of type {@link MouseCrosstab}.
     *
     * @return the loaded entities.
     */
    public Collection<MouseCrosstab> loadAll();

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform);

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int pageNumber, final int pageSize);

    /**
     * <p>
     * Does the same thing as {@link #loadAll(int)} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize);

    /**
     * Creates an instance of MouseCrosstab and adds it to the persistent store.
     * @param mouseCrosstab
     * @return MouseCrosstab
     */
    public MouseCrosstab create(MouseCrosstab mouseCrosstab);

    /**
     * <p>
     * Does the same thing as {@link #create(MouseCrosstab)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param mouseCrosstab
     * @return Object
     */
    public Object create(int transform, MouseCrosstab mouseCrosstab);

    /**
     * Creates a new instance of MouseCrosstab and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of MouseCrosstab
     * instances to create.
     *
     * @return the created instances.
     */
    public Collection<MouseCrosstab> create(Collection<MouseCrosstab> entities);

    /**
     * <p>
     * Does the same thing as {@link #create(MouseCrosstab)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param entities
     * @return Collection
     */
    public Collection<?> create(int transform, Collection<MouseCrosstab> entities);

    /**
     * <p>
     * Creates a new <code>MouseCrosstab</code>
     * instance from <strong>all</strong> attributes and adds it to
     * the persistent store.
     * </p>
     * @param mouse1 
     * @param mouse2 
     * @param mouse3 
     * @param mouse4 
     * @param mouse5 
     * @param mouse6 
     * @param mouse7 
     * @param mouse8 
     * @param mouse9 
     * @param mouse10 
     * @return MouseCrosstab
     */
    public MouseCrosstab create(
        Double mouse1,
        Double mouse2,
        Double mouse3,
        Double mouse4,
        Double mouse5,
        Double mouse6,
        Double mouse7,
        Double mouse8,
        Double mouse9,
        Double mouse10);

    /**
     * <p>
     * Does the same thing as {@link #create(Double, Double, Double, Double, Double, Double, Double, Double, Double, Double)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param mouse1 
     * @param mouse2 
     * @param mouse3 
     * @param mouse4 
     * @param mouse5 
     * @param mouse6 
     * @param mouse7 
     * @param mouse8 
     * @param mouse9 
     * @param mouse10 
     * @return MouseCrosstab
     */
    public Object create(
        int transform,
        Double mouse1,
        Double mouse2,
        Double mouse3,
        Double mouse4,
        Double mouse5,
        Double mouse6,
        Double mouse7,
        Double mouse8,
        Double mouse9,
        Double mouse10);

    /**
     * <p>
     * Creates a new <code>MouseCrosstab</code>
     * instance from only <strong>required</strong> properties (attributes
     * and association ends) and adds it to the persistent store.
     * </p>
     * @param mouseGroup
     * @return MouseCrosstab
     */
    public MouseCrosstab create(
        MouseGroup mouseGroup);

    /**
     * <p>
     * Does the same thing as {@link #create()} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform flag to determine transformation type.
     * @param mouseGroup
     * @return Object
     */
    public Object create(
        int transform,
        MouseGroup mouseGroup);

    /**
     * Updates the <code>mouseCrosstab</code> instance in the persistent store.
     * @param mouseCrosstab
     */
    public void update(MouseCrosstab mouseCrosstab);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     * @param entities
     */
    public void update(Collection<MouseCrosstab> entities);

    /**
     * Removes the instance of MouseCrosstab from the persistent store.
     * @param mouseCrosstab
     */
    public void remove(MouseCrosstab mouseCrosstab);

    /**
     * Removes the instance of MouseCrosstab having the given
     * <code>identifier</code> from the persistent store.
     * @param id
     */
    public void remove(Long id);

    /**
     * Removes all entities in the given <code>entities</code> collection.
     * @param entities
     */
    public void remove(Collection<MouseCrosstab> entities);


    /**
     * Does the same thing as {@link #search(int, Search)} but with an
     * additional two flags called <code>pageNumber</code> and <code>pageSize</code>. These flags allow you to
     * limit your data to a specified page number and size.
     *
     * @param transform the transformation flag.
     * @param pageNumber the page number in the data to retrieve
     * @param pageSize the size of the page to retrieve.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search wrapped in a {@link PaginationResult} instance.
     */
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search);

    /**
     * Does the same thing as {@link #search(Search)} but with an
     * additional two flags called <code>pageNumber</code> and <code>pageSize</code>. These flags allow you to
     * limit your data to a specified page number and size.
     *
     * @param pageNumber the page number in the data to retrieve
     * @param pageSize the size of the page to retrieve.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search wrapped in a {@link PaginationResult} instance.
     */
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search);

    /**
     * Does the same thing as {@link #search(Search)} but with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * finder results will <strong>NOT</strong> be transformed during retrieval.
     * If this flag is any of the other constants defined here
     * then results <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     *
     * @param transform the transformation flag.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search.
     */
    public Set<?> search(final int transform, final Search search);

    /**
     * Performs a search using the parameters specified in the given <code>search</code> object.
     *
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search.
     */
    public Set<MouseCrosstab> search(final Search search);

    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>mwk.pptp.androdomain.MouseCrosstabDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link mwk.pptp.androdomain.MouseCrosstabDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see #transformEntities(int,Collection)
     */
    public Object transformEntity(final int transform, final MouseCrosstab entity);

    /**
     * Transforms a collection of entities using the
     * {@link #transformEntity(int,MouseCrosstab)}
     * method. This method does not instantiate a new collection.
     * <p>
     * This method is to be used internally only.
     *
     * @param transform one of the constants declared in <code>mwk.pptp.androdomain.MouseCrosstabDao</code>
     * @param entities the collection of entities to transform
     * @see #transformEntity(int,MouseCrosstab)
     */
    public void transformEntities(final int transform, final Collection<?> entities);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse1 the unique mouse1 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse1(final Double mouse1);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse1 the unique mouse1 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse1(final int transform, final Double mouse1);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse2 the unique mouse2 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse2(final Double mouse2);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse2 the unique mouse2 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse2(final int transform, final Double mouse2);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse3 the unique mouse3 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse3(final Double mouse3);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse3 the unique mouse3 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse3(final int transform, final Double mouse3);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse4 the unique mouse4 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse4(final Double mouse4);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse4 the unique mouse4 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse4(final int transform, final Double mouse4);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse5 the unique mouse5 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse5(final Double mouse5);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse5 the unique mouse5 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse5(final int transform, final Double mouse5);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse6 the unique mouse6 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse6(final Double mouse6);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse6 the unique mouse6 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse6(final int transform, final Double mouse6);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse7 the unique mouse7 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse7(final Double mouse7);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse7 the unique mouse7 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse7(final int transform, final Double mouse7);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse8 the unique mouse8 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse8(final Double mouse8);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse8 the unique mouse8 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse8(final int transform, final Double mouse8);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse9 the unique mouse9 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse9(final Double mouse9);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse9 the unique mouse9 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse9(final int transform, final Double mouse9);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param mouse10 the unique mouse10 to be used in the search.'
     * @return a single instance of MouseCrosstab.
     */
    public MouseCrosstab searchUniqueMouse10(final Double mouse10);

    /**
     * Searches for a single instance of MouseCrosstab.
     * @param transform the transformation flag.
     * @param mouse10 the unique mouse10 to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueMouse10(final int transform, final Double mouse10);

    // spring-dao merge-point
}
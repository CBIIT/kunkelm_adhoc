// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.security.Principal;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import mwk.pptp.PrincipalStore;
import mwk.pptp.PropertySearch;
import mwk.pptp.Search;
import mwk.pptp.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>CellLineGroup</code>.
 * </p>
 *
 * @see CellLineGroup
 */
public abstract class CellLineGroupDaoBase
    extends HibernateDaoSupport
    implements CellLineGroupDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CellLineGroupImpl.class, id);
        return transformEntity(transform, (CellLineGroup)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup get(Long id)
    {
        return (CellLineGroup)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CellLineGroupImpl.class, id);
        return transformEntity(transform, (CellLineGroup)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup load(Long id)
    {
        return (CellLineGroup)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CellLineGroup> loadAll()
    {
        return (Collection<CellLineGroup>) this.loadAll(CellLineGroupDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(CellLineGroupDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(CellLineGroupImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup create(CellLineGroup cellLineGroup)
    {
        return (CellLineGroup)this.create(CellLineGroupDao.TRANSFORM_NONE, cellLineGroup);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final CellLineGroup cellLineGroup)
    {
        if (cellLineGroup == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.create - 'cellLineGroup' can not be null");
        }
        this.getHibernateTemplate().save(cellLineGroup);
        return this.transformEntity(transform, cellLineGroup);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CellLineGroup> create(final Collection<CellLineGroup> entities)
    {
        return (Collection<CellLineGroup>) create(CellLineGroupDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<CellLineGroup> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CellLineGroup entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup create(
        Double dose,
        Integer testNumber)
    {
        return (CellLineGroup)this.create(CellLineGroupDao.TRANSFORM_NONE, dose, testNumber);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        Double dose,
        Integer testNumber)
    {
        CellLineGroup entity = new CellLineGroupImpl();
        entity.setDose(dose);
        entity.setTestNumber(testNumber);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup create(
        CellLineType cellLineType,
        ConcentrationUnitType concentrationUnitType,
        ImplantSiteType implantSiteType,
        MouseType mouseType,
        MtdStudy mtdStudy,
        ScheduleType scheduleType,
        TreatmentRouteType treatmentRouteType,
        VehicleType vehicleType)
    {
        return (CellLineGroup)this.create(CellLineGroupDao.TRANSFORM_NONE, cellLineType, concentrationUnitType, implantSiteType, mouseType, mtdStudy, scheduleType, treatmentRouteType, vehicleType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        CellLineType cellLineType,
        ConcentrationUnitType concentrationUnitType,
        ImplantSiteType implantSiteType,
        MouseType mouseType,
        MtdStudy mtdStudy,
        ScheduleType scheduleType,
        TreatmentRouteType treatmentRouteType,
        VehicleType vehicleType)
    {
        CellLineGroup entity = new CellLineGroupImpl();
        entity.setCellLineType(cellLineType);
        entity.setConcentrationUnitType(concentrationUnitType);
        entity.setImplantSiteType(implantSiteType);
        entity.setMouseType(mouseType);
        entity.setMtdStudy(mtdStudy);
        entity.setScheduleType(scheduleType);
        entity.setTreatmentRouteType(treatmentRouteType);
        entity.setVehicleType(vehicleType);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(CellLineGroup cellLineGroup)
    {
        if (cellLineGroup == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.update - 'cellLineGroup' can not be null");
        }
        this.getHibernateTemplate().update(cellLineGroup);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<CellLineGroup> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CellLineGroup entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(CellLineGroup cellLineGroup)
    {
        if (cellLineGroup == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.remove - 'cellLineGroup' can not be null");
        }
        this.getHibernateTemplate().delete(cellLineGroup);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.remove - 'id' can not be null");
        }
        CellLineGroup entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<CellLineGroup> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineGroup.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>CellLineGroupDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link CellLineGroupDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see CellLineGroupDao#transformEntity(int, CellLineGroup)
     */
    public Object transformEntity(final int transform, final CellLineGroup entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case CellLineGroupDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case CellLineGroupDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see CellLineGroupDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link CellLineGroup}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof CellLineGroup)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return CellLineGroup
     */
    protected CellLineGroup toEntity(Object[] row)
    {
        CellLineGroup target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof CellLineGroup)
                {
                    target = (CellLineGroup)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CellLineGroupImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(CellLineGroupDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CellLineGroupImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<CellLineGroup> search(final Search search)
    {
        return (Set<CellLineGroup>) this.search(CellLineGroupDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup searchUniqueDose(final Double dose)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("dose",dose,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineGroup> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueDose(final int transform, final Double dose)
    {
        final CellLineGroup entity=this.searchUniqueDose(dose);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineGroup searchUniqueTestNumber(final Integer testNumber)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("testNumber",testNumber,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineGroup> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueTestNumber(final int transform, final Integer testNumber)
    {
        final CellLineGroup entity=this.searchUniqueTestNumber(testNumber);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}
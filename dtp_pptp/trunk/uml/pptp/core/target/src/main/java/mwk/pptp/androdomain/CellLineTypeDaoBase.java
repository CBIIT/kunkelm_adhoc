// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.security.Principal;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import mwk.pptp.PrincipalStore;
import mwk.pptp.PropertySearch;
import mwk.pptp.Search;
import mwk.pptp.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>CellLineType</code>.
 * </p>
 *
 * @see CellLineType
 */
public abstract class CellLineTypeDaoBase
    extends HibernateDaoSupport
    implements CellLineTypeDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CellLineTypeImpl.class, id);
        return transformEntity(transform, (CellLineType)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType get(Long id)
    {
        return (CellLineType)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(CellLineTypeImpl.class, id);
        return transformEntity(transform, (CellLineType)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType load(Long id)
    {
        return (CellLineType)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CellLineType> loadAll()
    {
        return (Collection<CellLineType>) this.loadAll(CellLineTypeDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(CellLineTypeDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(CellLineTypeImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType create(CellLineType cellLineType)
    {
        return (CellLineType)this.create(CellLineTypeDao.TRANSFORM_NONE, cellLineType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final CellLineType cellLineType)
    {
        if (cellLineType == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.create - 'cellLineType' can not be null");
        }
        this.getHibernateTemplate().save(cellLineType);
        return this.transformEntity(transform, cellLineType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<CellLineType> create(final Collection<CellLineType> entities)
    {
        return (Collection<CellLineType>) create(CellLineTypeDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<CellLineType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CellLineType entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType create(
        String pptpIdentifier,
        String name,
        Boolean secondary,
        String source,
        String dapIdentifier,
        Integer sortOrder,
        String masterbankidentifier,
        String masterbankcellname,
        String masterbankprimary,
        String masterbankcelltype,
        String masterbankpanel)
    {
        return (CellLineType)this.create(CellLineTypeDao.TRANSFORM_NONE, pptpIdentifier, name, secondary, source, dapIdentifier, sortOrder, masterbankidentifier, masterbankcellname, masterbankprimary, masterbankcelltype, masterbankpanel);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        String pptpIdentifier,
        String name,
        Boolean secondary,
        String source,
        String dapIdentifier,
        Integer sortOrder,
        String masterbankidentifier,
        String masterbankcellname,
        String masterbankprimary,
        String masterbankcelltype,
        String masterbankpanel)
    {
        CellLineType entity = new CellLineTypeImpl();
        entity.setPptpIdentifier(pptpIdentifier);
        entity.setName(name);
        entity.setSecondary(secondary);
        entity.setSource(source);
        entity.setDapIdentifier(dapIdentifier);
        entity.setSortOrder(sortOrder);
        entity.setMasterbankidentifier(masterbankidentifier);
        entity.setMasterbankcellname(masterbankcellname);
        entity.setMasterbankprimary(masterbankprimary);
        entity.setMasterbankcelltype(masterbankcelltype);
        entity.setMasterbankpanel(masterbankpanel);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType create(
        CellType cellType,
        String dapIdentifier,
        String masterbankcellname,
        String masterbankcelltype,
        String masterbankidentifier,
        String masterbankpanel,
        String name,
        PanelType panelType,
        String pptpIdentifier,
        String source)
    {
        return (CellLineType)this.create(CellLineTypeDao.TRANSFORM_NONE, cellType, dapIdentifier, masterbankcellname, masterbankcelltype, masterbankidentifier, masterbankpanel, name, panelType, pptpIdentifier, source);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        CellType cellType,
        String dapIdentifier,
        String masterbankcellname,
        String masterbankcelltype,
        String masterbankidentifier,
        String masterbankpanel,
        String name,
        PanelType panelType,
        String pptpIdentifier,
        String source)
    {
        CellLineType entity = new CellLineTypeImpl();
        entity.setCellType(cellType);
        entity.setDapIdentifier(dapIdentifier);
        entity.setMasterbankcellname(masterbankcellname);
        entity.setMasterbankcelltype(masterbankcelltype);
        entity.setMasterbankidentifier(masterbankidentifier);
        entity.setMasterbankpanel(masterbankpanel);
        entity.setName(name);
        entity.setPanelType(panelType);
        entity.setPptpIdentifier(pptpIdentifier);
        entity.setSource(source);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(CellLineType cellLineType)
    {
        if (cellLineType == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.update - 'cellLineType' can not be null");
        }
        this.getHibernateTemplate().update(cellLineType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<CellLineType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (CellLineType entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(CellLineType cellLineType)
    {
        if (cellLineType == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.remove - 'cellLineType' can not be null");
        }
        this.getHibernateTemplate().delete(cellLineType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.remove - 'id' can not be null");
        }
        CellLineType entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<CellLineType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "CellLineType.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>CellLineTypeDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link CellLineTypeDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see CellLineTypeDao#transformEntity(int, CellLineType)
     */
    public Object transformEntity(final int transform, final CellLineType entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case CellLineTypeDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case CellLineTypeDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see CellLineTypeDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link CellLineType}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof CellLineType)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return CellLineType
     */
    protected CellLineType toEntity(Object[] row)
    {
        CellLineType target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof CellLineType)
                {
                    target = (CellLineType)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CellLineTypeImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(CellLineTypeDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), CellLineTypeImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<CellLineType> search(final Search search)
    {
        return (Set<CellLineType>) this.search(CellLineTypeDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniquePptpIdentifier(final String pptpIdentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("pptpIdentifier",pptpIdentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniquePptpIdentifier(final int transform, final String pptpIdentifier)
    {
        final CellLineType entity=this.searchUniquePptpIdentifier(pptpIdentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueName(final String name)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("name",name,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueName(final int transform, final String name)
    {
        final CellLineType entity=this.searchUniqueName(name);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueSecondary(final Boolean secondary)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("secondary",secondary,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSecondary(final int transform, final Boolean secondary)
    {
        final CellLineType entity=this.searchUniqueSecondary(secondary);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueSource(final String source)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("source",source,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSource(final int transform, final String source)
    {
        final CellLineType entity=this.searchUniqueSource(source);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueDapIdentifier(final String dapIdentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("dapIdentifier",dapIdentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueDapIdentifier(final int transform, final String dapIdentifier)
    {
        final CellLineType entity=this.searchUniqueDapIdentifier(dapIdentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueSortOrder(final Integer sortOrder)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("sortOrder",sortOrder,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSortOrder(final int transform, final Integer sortOrder)
    {
        final CellLineType entity=this.searchUniqueSortOrder(sortOrder);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueMasterbankidentifier(final String masterbankidentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("masterbankidentifier",masterbankidentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueMasterbankidentifier(final int transform, final String masterbankidentifier)
    {
        final CellLineType entity=this.searchUniqueMasterbankidentifier(masterbankidentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueMasterbankcellname(final String masterbankcellname)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("masterbankcellname",masterbankcellname,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueMasterbankcellname(final int transform, final String masterbankcellname)
    {
        final CellLineType entity=this.searchUniqueMasterbankcellname(masterbankcellname);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueMasterbankprimary(final String masterbankprimary)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("masterbankprimary",masterbankprimary,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueMasterbankprimary(final int transform, final String masterbankprimary)
    {
        final CellLineType entity=this.searchUniqueMasterbankprimary(masterbankprimary);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueMasterbankcelltype(final String masterbankcelltype)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("masterbankcelltype",masterbankcelltype,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueMasterbankcelltype(final int transform, final String masterbankcelltype)
    {
        final CellLineType entity=this.searchUniqueMasterbankcelltype(masterbankcelltype);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CellLineType searchUniqueMasterbankpanel(final String masterbankpanel)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("masterbankpanel",masterbankpanel,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<CellLineType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueMasterbankpanel(final int transform, final String masterbankpanel)
    {
        final CellLineType entity=this.searchUniqueMasterbankpanel(masterbankpanel);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}
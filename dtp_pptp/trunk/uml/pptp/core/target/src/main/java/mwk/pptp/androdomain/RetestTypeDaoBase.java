// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.security.Principal;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import mwk.pptp.PrincipalStore;
import mwk.pptp.PropertySearch;
import mwk.pptp.Search;
import mwk.pptp.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>RetestType</code>.
 * </p>
 *
 * @see RetestType
 */
public abstract class RetestTypeDaoBase
    extends HibernateDaoSupport
    implements RetestTypeDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "RetestType.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(RetestTypeImpl.class, id);
        return transformEntity(transform, (RetestType)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType get(Long id)
    {
        return (RetestType)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "RetestType.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(RetestTypeImpl.class, id);
        return transformEntity(transform, (RetestType)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType load(Long id)
    {
        return (RetestType)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<RetestType> loadAll()
    {
        return (Collection<RetestType>) this.loadAll(RetestTypeDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(RetestTypeDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(RetestTypeImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType create(RetestType retestType)
    {
        return (RetestType)this.create(RetestTypeDao.TRANSFORM_NONE, retestType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final RetestType retestType)
    {
        if (retestType == null)
        {
            throw new IllegalArgumentException(
                "RetestType.create - 'retestType' can not be null");
        }
        this.getHibernateTemplate().save(retestType);
        return this.transformEntity(transform, retestType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<RetestType> create(final Collection<RetestType> entities)
    {
        return (Collection<RetestType>) create(RetestTypeDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<RetestType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "RetestType.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (RetestType entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType create(
        String compoundIdentifier,
        Double dose,
        String cellLineIdentifier,
        Integer testNumber)
    {
        return (RetestType)this.create(RetestTypeDao.TRANSFORM_NONE, compoundIdentifier, dose, cellLineIdentifier, testNumber);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        String compoundIdentifier,
        Double dose,
        String cellLineIdentifier,
        Integer testNumber)
    {
        RetestType entity = new RetestTypeImpl();
        entity.setCompoundIdentifier(compoundIdentifier);
        entity.setDose(dose);
        entity.setCellLineIdentifier(cellLineIdentifier);
        entity.setTestNumber(testNumber);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(RetestType retestType)
    {
        if (retestType == null)
        {
            throw new IllegalArgumentException(
                "RetestType.update - 'retestType' can not be null");
        }
        this.getHibernateTemplate().update(retestType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<RetestType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "RetestType.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (RetestType entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(RetestType retestType)
    {
        if (retestType == null)
        {
            throw new IllegalArgumentException(
                "RetestType.remove - 'retestType' can not be null");
        }
        this.getHibernateTemplate().delete(retestType);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "RetestType.remove - 'id' can not be null");
        }
        RetestType entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<RetestType> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "RetestType.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>RetestTypeDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link RetestTypeDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see RetestTypeDao#transformEntity(int, RetestType)
     */
    public Object transformEntity(final int transform, final RetestType entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case RetestTypeDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case RetestTypeDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see RetestTypeDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link RetestType}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof RetestType)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return RetestType
     */
    protected RetestType toEntity(Object[] row)
    {
        RetestType target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof RetestType)
                {
                    target = (RetestType)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), RetestTypeImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(RetestTypeDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), RetestTypeImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<RetestType> search(final Search search)
    {
        return (Set<RetestType>) this.search(RetestTypeDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType searchUniqueCompoundIdentifier(final String compoundIdentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("compoundIdentifier",compoundIdentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<RetestType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueCompoundIdentifier(final int transform, final String compoundIdentifier)
    {
        final RetestType entity=this.searchUniqueCompoundIdentifier(compoundIdentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType searchUniqueDose(final Double dose)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("dose",dose,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<RetestType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueDose(final int transform, final Double dose)
    {
        final RetestType entity=this.searchUniqueDose(dose);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType searchUniqueCellLineIdentifier(final String cellLineIdentifier)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("cellLineIdentifier",cellLineIdentifier,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<RetestType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueCellLineIdentifier(final int transform, final String cellLineIdentifier)
    {
        final RetestType entity=this.searchUniqueCellLineIdentifier(cellLineIdentifier);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public RetestType searchUniqueTestNumber(final Integer testNumber)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("testNumber",testNumber,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<RetestType> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueTestNumber(final int transform, final Integer testNumber)
    {
        final RetestType entity=this.searchUniqueTestNumber(testNumber);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}
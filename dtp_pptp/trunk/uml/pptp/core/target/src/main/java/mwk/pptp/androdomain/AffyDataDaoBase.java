// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: hibernate/SpringHibernateDaoBase.vsl in andromda-spring-cartridge.
//
package mwk.pptp.androdomain;

import java.security.Principal;
import java.util.Collection;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import mwk.pptp.PrincipalStore;
import mwk.pptp.PropertySearch;
import mwk.pptp.Search;
import mwk.pptp.SearchParameter;
import org.andromda.spring.PaginationResult;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>AffyData</code>.
 * </p>
 *
 * @see AffyData
 */
public abstract class AffyDataDaoBase
    extends HibernateDaoSupport
    implements AffyDataDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "AffyData.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(AffyDataImpl.class, id);
        return transformEntity(transform, (AffyData)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData get(Long id)
    {
        return (AffyData)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "AffyData.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(AffyDataImpl.class, id);
        return transformEntity(transform, (AffyData)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData load(Long id)
    {
        return (AffyData)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<AffyData> loadAll()
    {
        return (Collection<AffyData>) this.loadAll(AffyDataDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(AffyDataDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(AffyDataImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData create(AffyData affyData)
    {
        return (AffyData)this.create(AffyDataDao.TRANSFORM_NONE, affyData);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final AffyData affyData)
    {
        if (affyData == null)
        {
            throw new IllegalArgumentException(
                "AffyData.create - 'affyData' can not be null");
        }
        this.getHibernateTemplate().save(affyData);
        return this.transformEntity(transform, affyData);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<AffyData> create(final Collection<AffyData> entities)
    {
        return (Collection<AffyData>) create(AffyDataDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<AffyData> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "AffyData.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (AffyData entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData create(
        Integer statPairs,
        Integer statPairsUsed,
        Double signal,
        String detection,
        Double detectionPValue)
    {
        return (AffyData)this.create(AffyDataDao.TRANSFORM_NONE, statPairs, statPairsUsed, signal, detection, detectionPValue);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        Integer statPairs,
        Integer statPairsUsed,
        Double signal,
        String detection,
        Double detectionPValue)
    {
        AffyData entity = new AffyDataImpl();
        entity.setStatPairs(statPairs);
        entity.setStatPairsUsed(statPairsUsed);
        entity.setSignal(signal);
        entity.setDetection(detection);
        entity.setDetectionPValue(detectionPValue);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData create(
        AffyIdentifierType affyIdentifierType,
        CellLineType cellLineType,
        String detection)
    {
        return (AffyData)this.create(AffyDataDao.TRANSFORM_NONE, affyIdentifierType, cellLineType, detection);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        AffyIdentifierType affyIdentifierType,
        CellLineType cellLineType,
        String detection)
    {
        AffyData entity = new AffyDataImpl();
        entity.setAffyIdentifierType(affyIdentifierType);
        entity.setCellLineType(cellLineType);
        entity.setDetection(detection);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(AffyData affyData)
    {
        if (affyData == null)
        {
            throw new IllegalArgumentException(
                "AffyData.update - 'affyData' can not be null");
        }
        this.getHibernateTemplate().update(affyData);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<AffyData> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "AffyData.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (AffyData entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(AffyData affyData)
    {
        if (affyData == null)
        {
            throw new IllegalArgumentException(
                "AffyData.remove - 'affyData' can not be null");
        }
        this.getHibernateTemplate().delete(affyData);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "AffyData.remove - 'id' can not be null");
        }
        AffyData entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<AffyData> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "AffyData.remove - 'entities' can not be null");
        }
        this.getHibernateTemplate().deleteAll(entities);
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>AffyDataDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link AffyDataDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see AffyDataDao#transformEntity(int, AffyData)
     */
    public Object transformEntity(final int transform, final AffyData entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case AffyDataDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case AffyDataDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see AffyDataDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link AffyData}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof AffyData)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return AffyData
     */
    protected AffyData toEntity(Object[] row)
    {
        AffyData target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof AffyData)
                {
                    target = (AffyData)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), AffyDataImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(AffyDataDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), AffyDataImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<AffyData> search(final Search search)
    {
        return (Set<AffyData>) this.search(AffyDataDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData searchUniqueStatPairs(final Integer statPairs)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("statPairs",statPairs,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<AffyData> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueStatPairs(final int transform, final Integer statPairs)
    {
        final AffyData entity=this.searchUniqueStatPairs(statPairs);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData searchUniqueStatPairsUsed(final Integer statPairsUsed)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("statPairsUsed",statPairsUsed,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<AffyData> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueStatPairsUsed(final int transform, final Integer statPairsUsed)
    {
        final AffyData entity=this.searchUniqueStatPairsUsed(statPairsUsed);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData searchUniqueSignal(final Double signal)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("signal",signal,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<AffyData> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueSignal(final int transform, final Double signal)
    {
        final AffyData entity=this.searchUniqueSignal(signal);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData searchUniqueDetection(final String detection)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("detection",detection,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<AffyData> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueDetection(final int transform, final String detection)
    {
        final AffyData entity=this.searchUniqueDetection(detection);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public AffyData searchUniqueDetectionPValue(final Double detectionPValue)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("detectionPValue",detectionPValue,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<AffyData> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueDetectionPValue(final int transform, final Double detectionPValue)
    {
        final AffyData entity=this.searchUniqueDetectionPValue(detectionPValue);
        if(entity == null)
        {
            return null;
        }
        return transformEntity(transform, entity);
    }

    // spring-hibernate-dao-base merge-point
}